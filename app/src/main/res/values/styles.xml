<resources>
    <!-- Base application theme. -->
    <!-- Для использования Material эементом используем стиль унаследованных от Theme.MaterialComponents -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <!--
        <style name="StyleFragment" parent="Theme.MaterialComponents.DayNight">
            <item name="android:textSize">14dp</item>
            <item name="android:textColor">@color/design_default_color_primary</item>
            <item name="android:layout_margin"></item>
            <item name="android:fontFamily">cursive</item>
            <item name="android:textStyle">italic</item>
        </style>

        <style name="MyStyleForButton" parent="Theme.MaterialComponents.DayNight">
            <item name="android:textSize">14dp</item>
            <item name="android:textColor">@color/design_default_color_primary</item>
            <item name="android:layout_margin"></item>
            <item name="android:fontFamily">cursive</item>
            <item name="android:textStyle">italic</item>
            <item name="backgroundTint">@color/design_default_color_background</item>
        </style>
    -->
    </resources>